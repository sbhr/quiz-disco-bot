# AIエージェントへの指示書 (Agent Instructions)

このドキュメントは、AIエージェント（Antigravityなど）が `quiz-disco-bot` リポジトリで作業する際のコンテキスト、アーキテクチャのガイドライン、およびルールを提供します。コードベースに変更を加える前に必ず読んでください。

## プロジェクト概要
これは、ボイスチャンネルでの「早押し」形式のクイズを目的として設計されたDiscord Botです。
- データセットから問題を読み込みます。
- ボイスチャンネルでTTS（Text-to-Speech）を使用して問題を読み上げます。
- 早押しの割り込み処理には、DiscordのUI View（ボタン）を使用します。
- あいまい判定（Fuzzy Matching）に加え、**Gemini API（AI）を使用したセマンティックな正誤判定**をサポートしています。
- ジャンル選択、参加募集、クイズ中断などをボタンUIで行うことができます。
- スコアは永続化され、通算成績の確認が可能です。

## 技術スタック (Tech Stack)
- **Python:** 3.8以上
- **Discord フレームワーク:** `discord.py` (`[voice]` 依存関係を含む)
- **TTS:** `edge-tts` (高速・高音質なニューラル音声)。エラー時は自動で `gTTS` にフォールバックします。
- **データベース:** `sqlite3` (生涯成績の保存用)
- **あいまい判定:** `thefuzz` (および `python-Levenshtein`)
- **AI判定:** `google-genai` (Gemini API)
- **音声再生:** FFmpeg (`discord.FFmpegPCMAudio` 経由)

## アーキテクチャとディレクトリ構成
このアプリケーションは高度にモジュール化されています。この責務の分離は、将来的なバックエンドの差し替え（例: テキスト入力からSTTへ）を可能にするために非常に重要です。

- `/core/`: コアとなるビジネスロジック。
  - `session.py`: (`QuizSession`) **重要** クイズのメインループとゲーム状態を管理します。重複回避が有効でリセットが発生した際は、DiscordにリッチなEmbed通知を行います。
  - `question.py`: (`QuestionStore`) CSVからの問題の読み込み・取得に加え、既出問題の重複回避（SQLite永続化とオンメモリ高速チェックの組み合わせ）を処理します。初回起動時のログインポートや双方向の履歴同期ロジックもここにあります。
  - `score.py`: (`ScoreManager`) ユーザーのスコアを管理します。SQLiteによる永続化をサポートしています。
  - `voice.py`: (`VoiceManager`) `edge-tts` (または `gTTS`) による音声生成と、FFmpegによる再生を処理します。
  - `answer.py`: (`AnswerValidator` と `AnswerReceiver`) あいまい判定とテキスト入力の待機を処理します。AIバリデータと連携します。
  - `ai_validator.py`: (`AIAnswerValidator`) Gemini APIを使用して正誤を検証します。クォータ制限時のエラーハンドリングも行います。
- `/cogs/`: `discord.py` のCogの実装。
  - `quiz.py`: メインのQuizCog。セッションの開始・終了コマンドを管理します。
    - `RecruitView`: 参加募集。
    - `GenreSelectView`: ジャンル選択。
    - `FastestFingerView`: 早押しボタン。お手つき（ペナルティ）状態のチェックも行います。
- `/data/`: データストレージ（CSV問題、`quiz.db` SQLiteファイル）。
- `main.py`: エントリーポイント。

## ロードマップ (今後の展望)
1. **Speech-to-Text (STT):** テキスト入力のみに依存するのではなく、（Whisper APIなどを通じて文字起こしされた）音声入力を受け付けるように `core/answer.py` をアップグレードします。
2. **チーム対戦モード:** ユーザーをチームに分け、チーム合計スコアで競う機能。

## 開発上の重要なルールと注意点
1. **インタラクションのタイムアウト:** Discordのスラッシュコマンドには3秒以内に応答が必要です。時間のかかる処理（接続やAI判定など）の前には必ず `defer()` を使用してください。
2. **ビジュアル (Embed):** メッセージはプレーンテキストではなく、可能な限り `discord.Embed` を使用してリッチに演出してください。順位表、正解発表、統計表示などは視覚的なプレミアム感が求められます。
3. **ハイブリッド判定とフォールバック:** 
   - 処理効率とクォータ節約のため、高い一致度の場合はFuzzy判定で即答します。
   - Gemini APIがエラー（クォータ制限など）を返した場合、自動的に少し甘めのFuzzy判定 (`partial_ratio`) にフォールバックしてゲームを続行させます。
4. **ペナルティシステム:** 不正解時に解答を一時停止する「お手つき（frozen）」状態は `QuizSession` の `frozen_users` で管理されます。`FastestFingerView` でボタンが押された際にこの辞書をチェックしてください。
5. **環境変数:** すべてのトークンは `.env` から読み込む必要があります。
6. **参加者管理:** `registered_participants` が空の場合はVCにいる全員、そうでない場合は登録者のみに回答権が与えられます。
7. **出題重複回避 (SQLite ＋ オンメモリ):** 
   - `unique=True` (デフォルト) のとき、出題履歴は `data/quiz.db` の `used_questions` テーブルとメモリ上の `self.used_questions` 辞書で同期管理されます。
   - `all` (すべてのデータセット) プレイ時も `all` スコープで統一された履歴を管理し、全問出尽くした際にリセットされます。
   - **双方向同期**: あるジャンルで出題された問題は、そのジャンルの履歴と `all` の履歴の両方に同時登録され、シームレスな重複回避を実現します。
   - **初回ログインポート**: データベース初期化時に `used_questions` テーブルが空の場合のみ、過去ログ `logs/answer_validation.jsonl` から一括インポートして重複を排除します。2回目以降の起動ではスキップされ、起動速度の低下を防ぎます。
